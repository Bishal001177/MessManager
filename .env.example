# Vercel automatically sets POSTGRES_URL for you if you use the
# Vercel Postgres / Neon integration from the Storage tab.
# Only fill this in yourself if you're using an external Postgres
# provider (Neon, Supabase, Railway, etc.) directly.
POSTGRES_URL=postgres://user:password@host:5432/dbname
